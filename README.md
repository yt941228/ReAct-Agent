# ReAct Agent 静态分析实验

## 环境要求
- Ubuntu 20.04/22.04
- Python 3.8+
- radare2 (`sudo apt install radare2`)
- Ghidra (手动安装或snap: `sudo snap install ghidra --classic`)
- DeepSeek API Key (或兼容 OpenAI 的 API)
